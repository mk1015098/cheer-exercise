<div class='container px-5 px-md-0'>
   <div class='form-inline'>
    <div class='form-group mr-3 mb-3'>
      <% if user_signed_in? %>
        <div class="search_form">
          <%= form_with url: user_search_path, local: true, method: :get do |f| %>
            <%= f.text_field :word, placeholder: "キーワードを入力", class:"form-control" %>
            <%= f.submit "Search", name: 'body_search', class: "btn btn-primary" %>
          <% end %>
        </div>
      <% end %>
      <span>　</span>
      <% if user_signed_in? %>
        <div class="search_form">
          <%= form_with url: user_search_path, local: true, method: :get do |f| %>
            <%= f.text_field :word, placeholder: "タグ名を入力", class:"form-control" %>
            <%= f.submit "Tag Search", name: 'tag_search', class: "btn btn-primary" %>
          <% end %>
        </div>
      <% end %>
    </div>
  </div>
</div>